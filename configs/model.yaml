lightgbm:
  n_estimators: 300
  learning_rate: 0.05
  num_leaves: 31
  random_state: 42

lstm:
  seq_len: 20
  units: 64
  dropout: 0.2
  epochs: 15
  batch_size: 32

arima:
  order: [1, 1, 1]

stacking:
  meta_model: "linear"
